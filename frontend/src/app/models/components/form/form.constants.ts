/** Constants of the `app-form` design-system wrapper; the component extends this class. */
export class FormConstants {
  static readonly COLUMNS = [1, 2, 3, 4] as const;
  static readonly DEFAULT_COLUMNS = 2;
  static readonly GAP_PX = 12;
}
