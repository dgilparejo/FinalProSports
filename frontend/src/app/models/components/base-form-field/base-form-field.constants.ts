/** Constants of the `app-base-form-field` design-system wrapper; the component extends this class. */
export class BaseFormFieldConstants {
  static readonly APPEARANCE = 'outline' as const;
  static readonly SUBSCRIPT = 'dynamic' as const;
  static readonly DEBOUNCE_MS = 150;
}
