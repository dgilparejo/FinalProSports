/** Constants of the `app-textarea` design-system wrapper; the component extends this class. */
export class TextareaConstants {
  static readonly MIN_ROWS = 2;
  static readonly MAX_ROWS = 6;
  static readonly MAX_LENGTH = 2000;
}
