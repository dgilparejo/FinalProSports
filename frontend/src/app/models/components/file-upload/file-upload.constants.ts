/** Constants of the `app-file-upload` design-system wrapper; the component extends this class. */
export class FileUploadConstants {
  static readonly MAX_BYTES = 2_000_000;
  static readonly ACCEPT_TEXT = '.csv,.json,.txt';
  static readonly ACCEPT_JSON = '.json';
}
