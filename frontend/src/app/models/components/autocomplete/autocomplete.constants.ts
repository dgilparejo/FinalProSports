/** Constants of the `app-autocomplete` design-system wrapper; the component extends this class. */
export class AutocompleteConstants {
  static readonly MAX_OPTIONS = 12;
  static readonly MIN_CHARS = 1;
}
