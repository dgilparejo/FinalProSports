/** Constants of the `app-loading` design-system wrapper; the component extends this class. */
export class LoadingConstants {
  static readonly MODE = 'indeterminate' as const;
  static readonly HEIGHT_PX = 3;
}
