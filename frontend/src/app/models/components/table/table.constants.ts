/** Constants of the `app-table` design-system wrapper; the component extends this class. */
export class TableConstants {
  static readonly ROW_HEIGHT_PX = 34;
  static readonly ALIGNMENTS = ['left', 'right', 'center'] as const;
  static readonly EMPTY_KEY = 'common.empty';
}
