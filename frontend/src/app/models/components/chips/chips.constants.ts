/** Constants of the `app-chips` design-system wrapper; the component extends this class. */
export class ChipsConstants {
  static readonly TONES = ['neutral', 'accent', 'ok', 'warn', 'danger', 'rotated'] as const;
  static readonly DEFAULT_TONE = 'neutral';
}
