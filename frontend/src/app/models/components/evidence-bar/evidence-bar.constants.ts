/** Constants of the `app-evidence-bar` design-system wrapper; the component extends this class. */
export class EvidenceBarConstants {
  static readonly MIN_OPACITY = 0.2;
  static readonly MAX_OPACITY = 1;
  static readonly HEIGHT_PX = 6;
}
