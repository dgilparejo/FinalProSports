/** Constants of the `app-datepicker` design-system wrapper; the component extends this class. */
export class DatepickerConstants {
  static readonly ISO_LENGTH = 10;
  static readonly MIN_YEAR = 1920;
}
