/** Constants of the `app-stat-tile` design-system wrapper; the component extends this class. */
export class StatTileConstants {
  static readonly TONES = ['neutral', 'accent', 'ok', 'warn', 'danger'] as const;
  static readonly DEFAULT_TONE = 'neutral';
}
